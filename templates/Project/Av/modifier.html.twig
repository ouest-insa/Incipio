{% extends "Project/layout.html.twig" %}

{% set tab= "tab3" %}

{% block title %}
    {{ 'suivi.av_modifier_un'|trans({}, 'project') }} - {{ parent() }}
{% endblock %}

{% block content_title %}
    {{ 'suivi.av_modifier_un'|trans({}, 'project') }}
{% endblock %}

{% block breadcrumb_active %}
    {{ 'suivi.av_modifier_un'|trans({}, 'project') }}
{% endblock %}

{% block content_bundle %}

    {% include 'Project/Av/formulaire.html.twig' %}

    <br>

    <form class="bootbox-confirm" data-content="{{ 'suivi.avenant_supprimer_confirmation'|trans({}, 'project') }}"
          action="{{ path('project_av_supprimer', { 'id': form.vars.value.id }) }}" method="post">
        {{ form_widget(delete_form) }}
        <button class="btn btn-danger">{{ 'suivi.avenant_supprimer'|trans({}, 'project') }}</button>
    </form>

{% endblock %}

{% block javascript %}
    {{ parent() }}
    {{ form_javascript(form) }}
    <script>
        $(document).ready(function () {
            $('input.position').each(function (i) {
                $(this).val(i);
            });
            $('.titrePhase').each(function (i) {
                $(this).text('Phase n°' + (i + 1));
            });
            $(".select2-multiple").select2();
        });

    </script>
{% endblock %}
