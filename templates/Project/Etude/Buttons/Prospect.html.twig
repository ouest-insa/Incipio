{# Boutton Client :
 # Input : prospect
 # use : {% include "Project/Etude/Buttons/Prospect.html.twig" with  {'prospect':  etude.prospect} %}
 #}
<div class="btn-group" style="width: auto; min-width: 220px; max-width:500px;">
    <a title="{{ prospect.nom }}" class="btn btn-warning" href="{{ path('personne_prospect_voir', {'id': prospect.id}) }}" style="width : calc(100% - 26px); text-align: left; overflow: hidden;"><i class="icon-user icon-white"></i> {{ prospect.nom }}</a>
    <a class="btn btn-warning dropdown-toggle" data-toggle="dropdown" href="#"><span class="caret"></span></a>
    <ul class="dropdown-menu">
        <li><a href="{{ path('personne_prospect_voir', {'id': prospect.id}) }}"><i class="fa fa-info-circle" ></i> {{ 'suivi.voir'|trans({}, 'project') }}</a></li>
        <li><a href="#"><i class="fa fa-envelope" ></i> {{ 'suivi.contacter'|trans({}, 'project') }}</a></li>
        <li><a href="#"><i class="fa fa-comment" ></i> {{ 'suivi.commenter'|trans({}, 'project') }}</a></li>
        <li class="divider"></li>
        <li><a href="{{ path('personne_employe_ajouter', {'prospect_id': prospect.id}) }}"><i class="fa fa-plus" ></i> {{ 'suivi.employe_ajouter'|trans({}, 'project') }}</a></li>
        <li><a href="{{ path('personne_prospect_modifier', {'id': prospect.id}) }}"><i class="fa fa-pencil" ></i> {{ 'suivi.modifier'|trans({}, 'project') }}</a></li>
    </ul>
</div>
