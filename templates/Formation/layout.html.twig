{# Layout principal pour le formation bundle #}
{% extends "base.html.twig" %}

{% block title %}
    {{ 'formation.formations'|trans({}, 'formation') }} - {{ parent() }}
{% endblock %}

{% block content %}
    <div class="box box-primary">
        <div class="box-header with-border">
            <h3 class="box-title">
                {% block content_title %}
                    {{ 'formation.formations'|trans({}, 'formation') }}
                {% endblock %}
            </h3>

            {% block breadcrumb %}
                <ul class="breadcrumb no-margin pull-right">
                    <li>
                        <a href="{{ path('formations_lister') }}">
                            {{ 'formation.formations'|trans({}, 'formation') }}
                        </a>
                    </li>
                    {% block breadcrumb_items %}{% endblock %}
                    <li class="active">
                        {% block breadcrumb_active %}{% endblock %}
                    </li>
                </ul>
            {% endblock %}
        </div>

        <div class="box-body">
            {% block content_bundle %}
            {% endblock %}
        </div>

        <div class="box-footer"></div>
    </div>
{% endblock %}
